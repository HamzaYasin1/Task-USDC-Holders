const InfoMessages = {
	GENERIC: {
	  /**
	   * Returns a success message for getting an item.
	   * @param {string} key - The key or item name.
	   * @returns {string} The success message.
	   */
	  ITEM_GET_SUCCESSFULLY: (key) => `${key} get successfully.`,
	},
  };
  
  module.exports = {
	InfoMessages,
  };
  